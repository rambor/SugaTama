MESSAGE("** CURRENT SOURCE INCLUDE DIRECTORY: ${CMAKE_CURRENT_SOURCE_DIR}")

list(APPEND SOURCE_FILES
        "${CMAKE_CURRENT_LIST_DIR}/Aligner.cpp"
        "${CMAKE_CURRENT_LIST_DIR}/KDE.cpp"
        )

MESSAGE("** SOURCE FILES : ${SOURCE_FILES}")